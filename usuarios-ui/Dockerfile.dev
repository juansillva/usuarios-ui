# Etapa 1: build da aplicação
FROM node:18-alpine

# Diretório de trabalho dentro do container
WORKDIR /app

# Copiar package.json e package-lock.json antes para aproveitar cache
COPY package*.json ./

# Instalar dependências
RUN npm install

# Copiar o restante dos arquivos do projeto
COPY . .

# Expor a porta usada pelo dev server (geralmente 5173 no Vite ou 3000 no CRA)
EXPOSE 3000

# Comando para iniciar o projeto em modo dev
CMD ["npm", "run", "dev"]
